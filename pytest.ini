[pytest]
DJANGO_SETTINGS_MODULE = eventease.settings.dev
python_files = tests.py test_*.py *_tests.py
addopts =
    --strict-markers
    --tb=short
    --cov=.
    --cov-report=term-missing
    --cov-fail-under=70
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
